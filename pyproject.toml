[project]
name = "google-flights-mcp-server"
version = "0.1.0"
description = "Google Flights MCP Server !!"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.3.0",
]
